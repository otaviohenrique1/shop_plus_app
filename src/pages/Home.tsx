import { Center, Text, VStack } from "native-base";

export function Home() {
  return (
    <VStack flex={1}>
      <Center>
        <Text fontSize={40}>Home</Text>
      </Center>
    </VStack>
  );
}